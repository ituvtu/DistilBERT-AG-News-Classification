name: Keep Hugging Face Space Alive

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up HF Space
        run: |
          curl -I https://huggingface.co/spaces/ituvtu/DistilBERT-multi-text